// ctrl + enter - To pointer on next line
#include <stdio.h>
#include <math.h>

int main()
{

    int side;
    printf("Enter The value of side\n");
    scanf("%d", &side);
    printf("The value of area is %f", pow(side, 2)); //pow writtens double type value
    return 0;
}